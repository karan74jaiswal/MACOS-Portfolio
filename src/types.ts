export interface Navlink {
  id: number;
  name: string;
  type: string;
}
export interface NavIcon {
  id: number;
  img: string;
}

export interface FontWeight {
  min: number;
  max: number;
  default: number;
}
